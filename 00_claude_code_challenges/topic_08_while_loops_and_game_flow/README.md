# Topic 8 — While Loops & Game Flow

The final topic before the capstones. Days 11-14 skills: game
loops, state, and never-crash input handling.

NOTE: the order differs from the master challenge list on purpose —
`get_int()` comes FIRST here because every later challenge (and
both capstones) reuses it. Build your tool, then use your tool.

Recommended order:

| # | File | Skill focus |
|---|------|-------------|
| 8.1 | `01_get_int.py` | THE input validator — your first reusable module |
| 8.2 | `02_atm_pin.py` | attempts counter, loop exit conditions |
| 8.3 | `03_reverse_guessing.py` | binary search — the computer guesses |
| 8.4 | `04_best_of_five.py` | refactor day 4 RPS into a scored match |
| 8.5 | `05_hangman_v2.py` | fix 4 real issues in your day-7 hangman |

Done? Push, get the review, then claim your capstone. 🏁
