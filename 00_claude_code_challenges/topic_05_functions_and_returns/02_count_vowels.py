"""
CHALLENGE 5.2 — Count the Vowels
Topic: loop + accumulator INSIDE a function (Days 5+10)

BUILDS ON
---------
4.1 (accumulator pattern) — now wrapped in a returning function.
Day 8 love calculator — this is nearly the same loop you wrote there.

TASK
----
Write count_vowels(text) that returns how many vowels (a,e,i,o,u)
appear in the text. Case-insensitive: 'E' counts.

SAMPLE RUN
----------
Enter a sentence: Schuberg Philis runs On Python
That sentence has 8 vowels.

RULES
-----
1. Returns an int. No printing inside the function.
2. Handle uppercase WITHOUT listing 'AEIOU' too (one .lower() in
   the right place is cleaner than doubling your vowel string —
   where does it go?).
3. Prove it works: count the vowels in the sample yourself, THEN
   run it. If they disagree, one of you is wrong — find out which.
   (This predict-then-run habit is day-13 debugging discipline.)
"""

# --- write your code below this line ---
